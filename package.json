{
  "name": "reelforge",
  "version": "0.1.0",
  "description": "Web-based tool for creating Instagram Reels and TikTok videos from trend-based templates",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd src/backend && npm install && cd ../frontend && npm install && cd ../video && npm install && cd ../shared && npm install",
    "dev:backend": "cd src/backend && npm run dev",
    "dev:frontend": "cd src/frontend && npm run dev",
    "dev:video": "cd src/video && npm run studio",
    "build:backend": "cd src/backend && npm run build",
    "build:frontend": "cd src/frontend && npm run build",
    "build:video": "cd src/video && npm run build",
    "test:backend": "cd src/backend && npm test",
    "test:frontend": "cd src/frontend && npm test",
    "test:video": "cd src/video && npm run build"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
